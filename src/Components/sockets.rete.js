var Array = new Rete.Socket('Array');
var Number = new Rete.Socket('Number value');
var Object = new Rete.Socket('Object');
var sockets = {
    Array: Array,
    Number: Number,
    Object: Object
}

export default sockets;
